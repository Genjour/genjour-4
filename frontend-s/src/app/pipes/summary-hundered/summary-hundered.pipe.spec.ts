import { SummaryHunderedPipe } from './summary-hundered.pipe';

describe('SummaryHunderedPipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryHunderedPipe();
    expect(pipe).toBeTruthy();
  });
});
